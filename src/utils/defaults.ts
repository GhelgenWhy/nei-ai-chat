/**
 * Single source of truth for default storage paths.
 * Used by both settings defaults (main.ts) and runtime fallbacks (services).
 */
export const DEFAULT_CHATS_FOLDER = ".nei/chats";
export const DEFAULT_MEMORY_FILE = ".nei/memory.json";
export const DEFAULT_SKILLS_FOLDER = ".nei/skills";
export const DEFAULT_AGENT_RULES_FILE = ".nei/AGENTS.md";

export const DEFAULT_MAX_ATTACHMENT_SIZE_BYTES = 512000; // 500 KB
